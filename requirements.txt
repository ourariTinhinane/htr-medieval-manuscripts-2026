# HTR Medieval Manuscripts 2026 — Dépendances
# Généré pour Python 3.10+ / Google Colab

# ── Computer Vision & HTR ─────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0
datasets>=2.14.0
opencv-python-headless>=4.8.0
scikit-image>=0.21.0
Pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0

# ── Segmentation layout ───────────────────────────────────────────────────────
ultralytics>=8.0.0
huggingface_hub>=0.19.0
segment-anything>=1.0

# ── Export & formats ──────────────────────────────────────────────────────────
lxml>=4.9.0
shapely>=2.0.0

# ── Évaluation HTR ────────────────────────────────────────────────────────────
editdistance>=0.6.3
scikit-learn>=1.3.0
jsonschema>=4.19.0

# ── NLP ───────────────────────────────────────────────────────────────────────
peft>=0.6.0
accelerate>=0.24.0
networkx>=3.1

# ── POS / Lemmatisation (moyen français) ─────────────────────────────────────
# pie-extended>=0.3.0   # installer séparément : pip install pie-extended --break-system-packages
# stanza>=1.7.0         # fallback si pie-extended indisponible

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest>=7.4.0
